{
  "name": "@eon/scheduler-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node -r dotenv/config dist/main.js dotenv_config_path=../../.env",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured\"",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3"
  }
}
